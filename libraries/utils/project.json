{
  "name": "utils",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libraries/utils/src",
  "targets": {
    "lock": {
      "executor": "nx:run-commands",
      "options": {
        "command": "poetry lock",
        "cwd": "{projectRoot}"
      }
    },
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["poetry install"],
        "cwd": "libraries/utils"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["poetry run ruff check {projectRoot}/utils/"],
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "mkdir -p coverage/libraries/utils && COVERAGE_FILE=coverage/libraries/utils/.coverage poetry run pytest libraries/utils/ --cov-branch --cov-report xml:coverage/libraries/utils/coverage.xml --cov-report html:coverage/libraries/utils/html --junitxml=reports/libraries/utils/unittests/junit.xml --html=reports/libraries/utils/unittests/html/index.html",
          "poetry run python tools/assert_module_coverage.py --xml coverage/libraries/utils/coverage.xml --min 100 utils/services/db_probe.py utils/services/db_credentials.py"
        ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["poetry run mypy src/"],
        "cwd": "libraries/utils"
      }
    }
  },
  "tags": ["library", "python"]
}
