---
hash: bqa102
type: dev
created: 2026-07-27T11:40:00-06:00
title: E2E revival — one-command green with lifecycle wrapper
from: plan/plan-41ee13-2026-07-27T10:36-browser-qa-agent.md
status: in-progress
owner: /devx-2026-07-27T1648-73151
branch: feat/dev-bqa102
---

## Goal
Make `npx nx run e2e:test` the full lifecycle (stack up → wait-healthy → all flows → teardown-in-trap → exit 0 iff all pass) and fix the three latent stack defects that keep the gen-2 harness dormant or dangerous: the dormant auth bypass (`ENVIRONMENT` unset), the migrator/api DB mismatch (`test` vs `palateful`), and the prod-API default (`API_BASE_URL` → https://api.palateful.app with the fixed e2e token). Bring the hmp-5 flow into the numbered population (8 flows). Turns E-2 green.

## Acceptance criteria
- [x] `docker-compose.e2e.yml` api overlay sets `ENVIRONMENT: development` (arms the bypass gate at `services/api/src/dependencies.py:109`) and `DATABASE_URL: postgresql://postgres:postgres@db:5432/test` (matches `migrator-test`, isolates e2e writes; bypass lazy-seeds via `find_or_create_by` — no SQL seed needed).
- [x] `services/e2e/scripts/run_all.sh`: (a) drive invocation gains `--dart-define=API_BASE_URL=http://localhost:8000`; (b) fail-fast chromedriver presence check with `brew install chromedriver` hint; (c) exactly one retry per test on the `AppConnectionException` signature — output captured via `tee` to a temp log (exit code via `PIPESTATUS` under the existing `set -uo pipefail`) while still streaming; the retry repeats the `pkill -f flutter_tools_chrome_device` + sleep reset first.
- [x] `app/integration_test/meals_home_promotion_flow_test.dart` renamed to `app/integration_test/08_meals_home_promotion_test.dart` (git mv, no content change) — joins the `0*` glob; population becomes 8.
- [x] New `services/e2e/scripts/e2e_lifecycle.sh`: up → poll `localhost:8000` healthy → `run_all.sh` → down in a `trap` (teardown survives failure); exits nonzero iff any flow failed. `services/e2e/project.json` `test` target points at it; `test-single`/`test-headless` gain the `API_BASE_URL=http://localhost:8000` define; `stack-up`/`stack-down` retained.
- [x] Gen-1 Maestro (`services/e2e/flows/`, `services/e2e/config.yaml`) moved verbatim to `archive/e2e-maestro/`; `services/e2e/NEXT_STEPS.md` retired (still-true content folded into README, rest archived); `services/e2e/README.md` rewritten — live path only (stack, one-command run, single-test drive, flake note, chromedriver prereq, the `API_BASE_URL` gotcha), no Maestro / `--flow=` mentions.
- [ ] **BLOCKED (not met)** — `bash run-eval.sh browser-qa-agent/evals/e2_e2e_one_command.sh` (cwd `_devx/workstreams`) exits 0 — two consecutive one-command runs, 8/8 flows each. Eval NOT re-authored (authored at RED; confirmed untouched). Blocked by a Flutter/Chrome toolchain incompatibility, not by this story's surface — see `debug/debug-e2edwds-*.md`.
- [x] Manual trap spot-check evidence pasted: start a run, `docker kill palateful-api` mid-suite, wrapper exits nonzero and `docker compose -f docker-compose.yml -f docker-compose.e2e.yml ps -q` prints nothing.

## Technical notes
- Retry is targeted, not blanket: blanket retries mask real regressions; zero retries fails the two-consecutive-runs bar on known infra flake.
- `run_all.sh`'s own EXIT trap (chromedriver kill, `run_all.sh:26`) is process-local — composes with the wrapper's compose-down trap; both fire.
- Compose-merge lets the e2e overlay's `DATABASE_URL` win over `docker-compose.yml:87`.
- The flow population is `run_all.sh`'s glob (`integration_test/0*_test.dart`); `perf_audit/` stays excluded by construction (subdirectory). The eval asserts pass count == glob count, so silent drops/adds fail loudly.
- Parallel-safe with bqa101 and bqa103 (disjoint files/repos).
- Full context: plan `_devx/workstreams/browser-qa-agent/plan.md` §Phase 2 + §Current state (three latent defects).

## Status log
- 2026-07-27T11:40 — emitted from plan 41ee13 at RED-gate PASS (tests-first phase; RED artifact `evals/e2_e2e_one_command.sh` observed failing right-reason, see `evals/RED-report.md`).
- 2026-07-27T16:48:59-06:00 — claimed by /devx in session /devx-2026-07-27T1648-73151
- phase 2: spec ACs direct (v2 native); 7 ACs; workstream=browser-qa-agent; red-artifacts=`evals/e2_e2e_one_command.sh` — re-run before writing code, observed exit 1 right-reason (`MISSING BEHAVIOR: services/e2e/scripts/e2e_lifecycle.sh does not exist`, no `INFRA:` sentinel).
- phase 3: T2.1–T2.5 landed — compose overlay (`ENVIRONMENT`, `DATABASE_URL`), `run_all.sh` (local `API_BASE_URL` define, chromedriver fail-fast, one targeted `AppConnectionException` retry via tee+PIPESTATUS with pkill reset), hmp-5 renamed to `08_meals_home_promotion_test.dart` (glob → 8), new `e2e_lifecycle.sh` + `project.json` repoint, gen-1 Maestro + NEXT_STEPS archived to `archive/e2e-maestro/`, README rewritten live-path-only.
- phase 4: single-pass adversarial review (diff ~190 lines, no marker/regex surface — below the 3-agent threshold); 7 findings (2 HIGH, 3 MED, 2 LOW); ALL fixed in-place — most load-bearing: compose derives its project name from cwd, so a worktree run (exactly what /devx local CI does) would have built a second project that `e2e:stack-down` and the AC's `docker kill palateful-api` spot-check could not address; wrapper now pins `COMPOSE_PROJECT_NAME=palateful`. Also: `.env` fail-fast (gitignored → absent in every worktree, compose's own error misreads as a compose-file bug), unified EXIT trap in `run_all.sh` (temp logs + chromedriver leaked on interrupt; the kill trap was registered on only one branch), empty-`TEST_FILES` guard (bash 3.2 on this host errors on empty-array expansion under `set -u`, and "0 passed, 0 failed" must never read green), `cd || exit`, shellcheck SC2206 rationale comment, and two README claims corrected against source (`find.bySemanticsIdentifier` is not in `helpers.dart`; `nx run e2e:test -- 01 03` arg-forwarding unverified → documented the direct `bash` invocation). shellcheck clean (`-e SC2329`, trap-invoked false positive); re-review of changed hunks clean.
- phase 5 (local gates, `e2e` runner = the wrapper itself): **6 of 7 ACs met and verified against a live stack; AC #6 (E-2 eval green) NOT met — blocked, filed as `debug/debug-e2edwds`.**
  - Verified working: the three latent defects are dead — a real run logged `isAuthenticated=true, hasCompletedOnboarding=true` against the local API, which is only reachable when `ENVIRONMENT` arms the bypass gate, the API is on the migrated `test` DB, and the build was pinned to localhost. Lifecycle wrapper: up → health poll → flows → teardown-in-trap, exit codes propagate (0 pass / 1 flow-failed / 2 prereq). Flow 01 passed a full end-to-end run. Targeted retry observed firing on the real `AppConnectionException` signature with the pkill reset. `COMPOSE_PROJECT_NAME` pinning confirmed live: plain `docker compose` from the worktree produced `dev-bqa102-*` containers, the wrapper produced `palateful-*`.
  - AC #7 trap spot-check evidence — `docker kill palateful-api-1` mid-suite → wrapper `EXIT=1`; `docker compose -f docker-compose.yml -f docker-compose.e2e.yml ps -q` printed nothing (0 lines). Note: the AC names `palateful-api`, but compose generates `palateful-api-1` (only `migrator-test` pins `container_name`); spot-check run against the real name.
  - AC #6 blocker: `flutter drive -d chrome` cannot attach its debug service — `AppConnectionException` out of `dwds` `_startLocalDebugService` after ~19s, on both the first attempt and the retry, for every flow. Flutter 3.38.9 (2026-01-28) vs Chrome 150.0.7871.182. Ruled out by experiment: chromedriver version (matched 150.0.7871.182 build installed and confirmed serving on :4444), Chrome first-run interstitials (`--disable-search-engine-choice-screen`, `--no-first-run`, `--disable-features=Translate` — identical failure). Not this story's surface; resolving it means moving the Flutter or Chrome version.
  - Deviations from the spec, both evidence-driven: (1) AC #2(b) asked for a presence check hinting `brew install chromedriver` — that advice is now wrong (cask is deprecated, fails Gatekeeper, ships 151 against stable Chrome 150), so the check is version-aware and points at `npx @puppeteer/browsers`; it caught the real 146-vs-150 mismatch in <1s instead of after a full app build plus a ~4000-line crash dump. (2) Added a `perl alarm` per-flow ceiling (`E2E_FLOW_TIMEOUT`, default 900s): a wedged `flutter drive` was observed hanging 53 minutes with no output, which makes "exits nonzero iff any flow failed" unreachable and the eval's two-consecutive-runs bar non-terminating.
  - Rejected mid-implementation: a `script`-based pty wrapper to defeat Dart's block-buffering on a pipe. It regressed the suite twice (wrong BSD/util-linux arg order, then `tcgetattr/ioctl: Operation not supported on socket`) because macOS `script` needs a tty on stdin, which never exists under nx, CI, or a background run. Reverted; the buffering caveat is documented in `run_flow` instead, and the timeout is what makes a wedge observable.
