name: palateful
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.58+71

environment:
  sdk: '>=3.7.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  auth0_flutter: ^1.14.0
  dio: ^5.9.0
  go_router: ^17.0.0
  get_it: ^9.2.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  wakelock_plus: ^1.2.1
  web_socket_channel: ^3.0.1
  firebase_core: ^3.9.0
  firebase_crashlytics: ^4.3.10
  firebase_messaging: ^15.2.0
  # cla-11: Firebase Performance Monitoring — secondary cross-check
  # against the custom client_latencies pipeline. Scoped to HTTP +
  # app-start (cla-12 disables auto screen-rendering traces). 0.10.0+11
  # is the last 0.10.x line compatible with firebase_core ^3.x; newer
  # 0.11.x requires firebase_core ^4. flutter_web_release may tree-shake
  # on dart2js — soft-fail fallback per AC3 (mobile-only is acceptable).
  firebase_performance: ^0.10.0+11
  image_picker: ^1.0.7
  http: ^1.2.0
  # Riverpod 3.x dev required: stable 2.x depends on build ^2.0.0 which conflicts
  # with freezed 3.2.3 (requires build >=3.0.0). Move to stable when Riverpod 3.0 releases.
  flutter_riverpod: ^3.0.0-dev.3
  riverpod_annotation: ^3.0.0-dev.3
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  url_launcher: ^6.2.5
  receive_sharing_intent: ^1.8.0
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.4
  flutter_timezone: ^1.0.4
  shared_preferences: ^2.3.4
  connectivity_plus: ^6.1.4
  share_plus: ^10.1.4
  file_picker: ^10.3.10
  home_widget: ^0.7.0
  live_activities: ^2.0.0
  path_provider: ^2.1.5
  # sru-1: sha256 for (path+mtime+size) dedup-key used by the receiving
  # screen + its s3_key suffix. Already transitive via firebase_auth,
  # pinning it directly so the import site is explicit.
  crypto: ^3.0.3
  # cla-3: read version+build at boot so `ClientLatencyIngest` can tag
  # every outbound event with the correct `app_version`. Transitive via
  # firebase_core, pinned explicitly so the import site is obvious.
  package_info_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  freezed: ^3.2.3
  build_runner: ^2.10.5
  json_serializable: ^6.11.2
  riverpod_generator: ^3.0.0-dev.3
  # arh-3: generates adaptive launcher icons (mipmap-anydpi-v26 + raster
  # fallback + monochrome) from a single 1024x1024 source PNG. Re-run
  # with `dart run flutter_launcher_icons` after editing the source.
  flutter_launcher_icons: ^0.14.4
  # rmc-4: deterministic timer tests for the MealEventCoalescer — no
  # wall-clock waits in CI. Transitive via `test`, pinned explicitly
  # so the import site is obvious.
  fake_async: ^1.3.1

# arh-3: Adaptive launcher icon generator. Source-of-truth PNG for both
# Android launcher (mipmap-anydpi-v26 + raster fallbacks + monochrome)
# and the 512x512 Play Store listing icon (downscaled by apl-2). Cream
# background (#FAF7F2) matches the app's main surface colour.
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "android/play-store-assets/icon-source-1024.png"
  min_sdk_android: 26
  adaptive_icon_background: "#FAF7F2"
  adaptive_icon_foreground: "android/play-store-assets/icon-source-1024.png"
  adaptive_icon_monochrome: "android/play-store-assets/icon-source-1024.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
