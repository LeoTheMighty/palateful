# Cloudflare Pages redirect rules. Format: <source> <destination> <status>
# Intentionally empty: Pages auto-strips .html, so /privacy already
# serves privacy.html as 200. An explicit rewrite to /privacy.html
# loops with auto-strip (308 /privacy → rewrite → 308 /privacy → …).
