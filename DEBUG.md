# DEBUG — Bugs to fix

Conventions: `[ ]` ready · `[/]` in-progress · `[-]` blocked · `[x]` done. Status field on each entry is the source of truth; checkbox is the glanceable mirror.

## Imported from BUGS.md (2026-07-27)

- [/] `debug/debug-rbv101-2026-07-27T17:30-book-view-card-sizing.md` — Recipe-book view renders meal and recipe cards at different sizes. Status: in-progress. From: BUGS.md (newest report).
- [x] `debug/debug-btri01-2026-07-27T17:31-legacy-bugs-triage.md` — Triage legacy BUGS.md reports against current main (meal creation, push notifs, auth0 logout, shopping cart, token refresh — all likely fixed by bas-1..4 + landed epics); close with evidence or file real debug specs. Status: done. PR: https://github.com/LeoTheMighty/palateful/pull/2 (merged `0aff93a7`).

## Found during /devx runs (2026-07-27)

- [x] `debug/debug-imptab1-2026-07-27T18:50-imports-tab-sections-not-rendering.md` — Imports tab: 3 widget tests red on `main` (`imports_tab_test.dart`). Root cause: **test fixture rot, not a code regression** — fixtures hardcoded `2026-04-18` and aged past the 30-day recency cutoff at `imports_tab.dart:168`, which suppresses `completed`/`skipped` items only. Fixtures re-anchored to `now`; assertions unchanged. Status: done. PR: https://github.com/LeoTheMighty/palateful/pull/6 (merged 640987e). ~~May be a user-visible regression~~ — it is not; the cutoff is intended behaviour with See-all as the escape hatch.
- [x] `debug/debug-dvxci1-2026-07-27T18:35-devx-ci-npm-test-missing-script.md` — `devx-ci` `test` job red on every run (incl. `push` to `main`): `npm test --silent` with no `test` script in package.json; sibling lint/coverage jobs guard with `--if-present`, test did not. One-line fix landed via PR #6 (merged 640987e) — ported there byte-identically because dvxci1 and imptab1 were a mutual block. Status: done. **Open question still unresolved**: whether the TS/JS `devx-ci` should exist at all in a Python/Flutter repo where `ci.yml` already runs the real suites.
- [x] `debug/debug-rshred1-2026-07-27T21:35-rotation-red-artifacts-break-main-pytest.md` — `ci.yml / test` (pytest) red on `main` since `5a6174d` (rotation-self-heal RED stage): `ModuleNotFoundError: No module named 'utils.services.db_credentials'` at collection. Status: done. From: bqa101 Phase 8 post-merge check. Resolved by the registry-driven policy — a RED artifact may merge but must be registered in `tools/red-artifacts.txt` in the same commit, and registered paths drop out of default collection via a `collect_ignore` conftest (`libraries/utils/test/conftest.py`); `PYTEST_RUN_RED=1` opts back in. The commit that turns an artifact GREEN must delete its registry line. PR: https://github.com/LeoTheMighty/palateful/pull/9 (merged `bac7d6b9`). Verified 2026-09-20: `main` green at `e212dd25` (run 30655623108, `test`=success), deploy lane reopened (run 30646967338 full-lane success).
- [ ] `debug/debug-covcomb1-2026-09-20T13:10-parallel-pytest-targets-share-coverage-data.md` — Parallel pytest targets share one coverage data file: `utils`, `test-helper` and `migrator` all run pytest from `{workspaceRoot}`, so `nx affected -t test --parallel=3` combines their `.coverage.*` files together. Adding `--cov-branch` to one of them makes combine raise `DataError: Can't combine branch coverage data with statement data`. Status: ready. From: rsh102 Phase 7 (PR #29 red while every local gate was green — running one target alone never exercises the collision). rsh102 fixed `utils` via COVERAGE_FILE (`6d375e71`); the siblings still share.

- [x] `debug/debug-arci1-2026-07-27T23:10-await-remote-ci-single-workflow-blind-spot.md` — `devx devx-helper await-remote-ci` reports terminal `success` when the *newest* run is green, ignoring a sibling workflow that failed on the same `headSha`. Observed on `408aeaf`: the helper said success (`CI & Deploy`) while `devx-ci` was red and `devx merge-gate` correctly said `merge:false`. Defense-in-depth failure, not an incident — the gate caught it — but Phase 7 is documented as the place that waits for CI. Status: done. From: rsh101 Phase 8. PR: https://github.com/LeoTheMighty/palateful/pull/10

- [x] `debug/debug-e2edwds-2026-07-27T19:00-dwds-chrome150-attach-failure.md` — **RESOLVED 2026-07-30 by `dev/dev-fltup1`: the suspected toolchain skew was the cause.** Local Flutter 3.38.9 → 3.41.7 makes the debug service attach (`Debug service listening on ws://127.0.0.1:50013/...` at 25.5s; `grep -c AppConnectionException` → 0), against the same Chrome 150.0.7871.187 and the same exact-match chromedriver. No Chrome downgrade needed; CI pins untouched. Status: done. **The E-2 eval is still not green** — flow 01 now gets past launch and fails on a DI defect this bug was masking, filed as `debug/debug-e2egetit`. That is bqa102's new blocker. Original report follows: `flutter drive -d chrome` cannot attach its dwds debug service against Chrome 150: `AppConnectionException` out of `_startLocalDebugService` after ~19s, on both the first attempt and the targeted retry, for every flow. Flutter 3.38.9 (2026-01-28) vs Chrome 150.0.7871.182 — suspected toolchain skew. Ruled out by experiment: chromedriver version (exact-match 150 build confirmed serving on :4444) and Chrome first-run interstitials. **Blocks bqa102's headline AC** (E-2 eval: two consecutive 8/8 one-command runs) and therefore Phase 2 of the browser-qa-agent workstream. The e2e harness itself is verified working — this is a browser/toolchain bisect, not app or harness code. Status: ready. **First bisect arm filed as `dev/dev-fltup1` (local Flutter 3.38.9 → CI's pinned 3.41.7)** — that ticket must answer the dwds question either way and report back here. From: bqa102 Phase 5.

## Found during /devx fltup1 (2026-07-30)

- [x] `debug/debug-e2egetit-2026-07-30T13:00-clientlatencyingest-not-registered-in-e2e-mode.md` — Under `E2E_MODE=true` the app crashes on its first frame: `Bad state: GetIt: Object/factory with type ClientLatencyIngest is not registered inside GetIt`, thrown in a scheduler callback from `app_router.dart:98` via `perf_navigator_observer.dart:87`. The router never finishes building, so the follow-on `Found 0 widgets with text "Home"` is a consequence, not a second defect. The 1564-test widget suite is green on the same commit and toolchain, so the gap is specific to the E2E boot path. **Successor blocker to `debug-e2edwds` — this is what bqa102's E-2 eval now waits on.** Status: done. From: fltup1 AC #5. PR: https://github.com/LeoTheMighty/palateful/pull/18
- [x] `debug/debug-nxappproj-2026-07-30T13:05-flutter-app-not-registered-with-nx.md` — The Flutter app is not an nx project: `npx nx show projects` lists 11 projects without `app`, there is no `app/project.json` anywhere, and `npx nx run app:test` exits 1 with `Cannot find project 'app'` from any directory. CLAUDE.md mandates nx-first, and specs keep being written against `app:*` targets that cannot run (fltup1 AC #2 was one). Low severity, but it makes a missing-project error look like a failing suite. Status: done. PR: https://github.com/LeoTheMighty/palateful/pull/19 (merged `700a131d`). From: fltup1 AC #2.

## Found during import verification (2026-07-27)

- [x] `debug/debug-rcres1-2026-07-27T18:21-deleted-occurrence-resurrection.md` — Deleted recurring-meal occurrence resurrects on next materialize pass (delete detaches the row from its rule → invisible to the materializer's dedup → slot re-inserts; also `is_recurring` never set on materialized rows). Status: done. PR: https://github.com/LeoTheMighty/palateful/pull/4 (merged `98d19f11`). From: bugs-cal-3b verification.

## Pre-existing red suite (2026-07-27)

- [x] `app/test/features/activity/imports_tab_test.dart` — 3 failing widget tests on `feat/dev-ifh4` (branched from main), untriaged, no spec file yet: "renders all four sections with one row each" (expects text `Auto-Imported · 1`, finds 0 widgets, line 294), "green row taps navigate to /recipes/:id", and "buckets by item.status — items still render when parent job.status differs" (both tap a `Ship it` row that never renders, line 456). Looks like the Auto-Imported section stopped rendering in `lib/features/activity/imports_tab.dart`. Status: ready. From: ifh-4 full-suite verification — the file has no dependency on `pending_imports_reconciler.dart`, so these are unrelated to that story but will keep CI red until fixed. **Closed 2026-09-22 as duplicate of imptab1 / imptb1, no spec filed.** Re-run on `main` @ `faf35fa1` (clean `build/unit_test_assets`): `flutter test test/features/activity/imports_tab_test.dart` → exit 0, `+8: All tests passed!`, all three named tests included. Cause was the date-rotted fixtures fixed in 640987e5 (PR #6); fixtures are now anchored to `now` (`imports_tab_test.dart:147`). `feat/dev-ifh4` was branched before that fix.

## Found during btri01 legacy triage (2026-07-27)

- [/] `debug/debug-lgort1-2026-07-27T17:41-auth0-logout-returnto-malformed.md` — Native Auth0 logout `returnTo` puts `Environment.auth0Scheme` (`com.palateful.app`) in the path segment where the bundle id / package name (`com.palateful.palateful`) belongs, so it can't be in Allowed Logout URLs → Auth0 error page. bas-1 did not fix the "weird auth0 page" report; the SDK's own default `returnTo` would have been correct. Status: in-progress. From: btri01 (BUGS.md "Logging out shows a weird auth0 page").
- [/] `debug/debug-d19992-2026-09-22T10:00-auth-login-failed-and-session-not-holding.md` — Leo's "random 'Login failed'" and "credentials don't hold": both device-side and never observable (zero 401s in 30 days; no ErrorReporter call in the auth files). Restore wiped a good refresh token on `RENEW_FAILED`, which the SDK raises for any failed renewal incl. no connectivity; `login()` swallowed every error into one message; the account-linking Action runs unguarded Management API calls on every login. Dashboard half for Leo: iOS logout URL, Android callback URL, Rotation Overlap ≥ 60 s, guarded Action. Status: in-progress. From: lgort1 / auth hardening track.
- [ ] `debug/debug-andph1-2026-09-20T1030-auth0-android-manifest-placeholders.md` — `auth0_flutter`'s `RedirectActivity` library manifest references `${auth0Domain}` / `${auth0Scheme}` placeholders that `app/android/app/build.gradle.kts` never defines, and no CI job builds Android, so nothing proves the manifest merge succeeds. Status: ready. From: lgort1 (found while fixing the logout `returnTo`; orthogonal — lgort1 is the string sent to `/v2/logout`, this is whether Android can receive the redirect at all).
- [ ] `debug/debug-cldb01-2026-07-27T17:41-client-latencies-db-auth-500.md` — Live prod 500 on `POST /v1/client-latencies`: `password authentication failed for user "palateful"` (2026-07-27 16:49 UTC, one occurrence). Corroborate against CloudWatch or close as transient. Status: ready. From: btri01 evidence gathering.
- [x] `debug/debug-imptb1-2026-07-27T18:53-imports-tab-widget-tests-red-on-main.md` — `flutter test` is red on `main`: 3 widget tests in `app/test/features/activity/imports_tab_test.dart` fail (expected rows / section copy not rendering). 1530 pass, 3 fail. Pre-existing, unrelated to btri01. Status: done. PR: https://github.com/LeoTheMighty/palateful/pull/26 (merged `65d4d75a`). From: btri01 full-suite verification.


## Found during deploy-freshness auth fix (2026-09-20)

- [ ] `debug/debug-prstrnd-2026-09-20T10:15-finished-work-strands-in-conflicted-pr-undetected.md` — Nothing watches the open-PR queue for staleness, so a PR holding finished, live-verified work can sit unmergeable and silent indefinitely. PR #24 did exactly that for 51 days (`CONFLICTING`/`DIRTY`, empty check rollup, `updatedAt == createdAt`), and what it held was the fix for `deploy-freshness` — so the freeze detector was blind *and* its repair was invisible, the whole time prod was actually frozen. Sole conflict was the append-only devx Status log in a markdown spec; both workflow files merged clean. The pre-fix check fired 50 scheduled runs between 2026-08-01 and 2026-09-19 and all 50 failed — it never measured prod once, so the detector was dead for exactly the window it existed to cover — and 25 of those 49 firing intervals exceeded 24h (max 31.95h), so the schedule could not have honoured its own requirement even alive. Detector gap, not a merge-queue request — flag stranded PRs, don't auto-resolve them; 50 consecutive identical failures that nobody consumed suggests the same detector should also catch permanently-red scheduled workflows. Status: ready. From: deploy-freshness auth fix (PR for `fix/deploy-freshness-env-secrets`).

## Found during Leo's hardening initiative (2026-09-22)

- [ ] Local test runs across worktrees share ONE Postgres container — a red local run is not evidence until it is repeated. Observed 2026-09-22 (palateful-30, covcomb1 re-verification): the first `nx run-many -t test --projects=utils,test-helper,migrator --parallel=3` after a rebase failed in `libraries/utils/test/test_db_credentials_live_drivers.py` with `server closed the connection unexpectedly` and `Connection reset by peer`; `utils:test` alone then passed 738, and three further parallel runs passed 3/3. The container is `selfheal1-pg` (pgvector/pg16), started by another session and shared by every worktree on this machine, so another session restarting or re-seeding it lands mid-run in whoever else is testing. **The inverse is the worse half**: a green local run can be riding another session's schema or seed data. Status: ready. From: leonidbelyi-41 after the covcomb1 rebase. Candidate fixes: a per-worktree container with a per-worktree port, or a CI-only policy for the live-driver tests.

- [/] `debug/debug-cartdec-2026-09-22T09:00-shopping-cart-decimal-quantity-crash.md` — **The shopping cart has been unusable since at least April: every cart response sends `quantity` as a JSON string (Pydantic v2 `Decimal`) and the app's `as num?` crashes parsing it.** Server returns 200 and logs nothing; the only trace was a client `_TypeError` (area shopping.cart / loadList). A May fix (a5c84386) landed on a module no endpoint used, passed its own test, deployed, and did nothing; btri01 then closed the bug in July on another proxy. All ten cart endpoints measured failing. Status: in-progress. From: btri01 (reopened by evidence).
- [ ] `debug/debug-storesec-2026-09-22T09:00-store-sections-route-shadowed.md` — `GET /shopping-lists/store-sections` is unreachable: `/{list_id}` is declared first and captures it. Latent — the app doesn't call it. Status: ready. From: cartdec.
- [ ] `debug/debug-apisweep-2026-09-22T09:00-unauthenticated-api-route-sweep.md` — Unauthenticated sweeps of ~65 prod endpoints in ~5 s (2026-09-11, 2026-09-14). Source unknown. Status: ready. From: cartdec (measured by palateful-4f).
- [ ] `debug/debug-impprog1-2026-09-22T23:00-blue-rows-always-say-zero-processed.md` — every In Progress row reads "Importing 0 of N" because `ListImportJobs.JobSummary` never sends `processed_items`; the widget tests invented the field, so nothing caught it. Status: ready. From: impvis1 review.
- [-] `debug/debug-parsercap1-2026-09-23T00:00-photo-import-pipeline-dead-no-spot-capacity.md` — photo import has completed nothing since April; tonight's Batch job never started and failed on `instance-terminated-no-capacity` ×3 (spot-only GPU pool, no on-demand fallback). The Activity tab is empty because `import_jobs` is empty — the rows were never created. Status: ready. From: impvis1 / measured by palateful-4f, needs independent re-confirmation. **Superseded 2026-09-23**: capacity + permanent-vs-unlucky + the independent re-confirmation AC → `dev/dev-pcap1`; write-back + April stale state → `dev/dev-prcon1`. Split by owner so no AC is dropped.
- [ ] `debug/debug-copydrift1-2026-09-23T03:30-copy-grep-allowlist-pins-line-numbers.md` — **three guards, one trap, two halves.** `file:lineno` allowlists rot when anything is inserted above an entry (live: acttab1 moved one 865→872 and CI went red) and their colon-split parsers break on a rationale that cites a line number (hit in #47). `copy-grep-guard` has the first, `silent-catch-allowlist.txt` has both and is latent, `stale-pointer-check.py` had the second. Only PR #56's core baseline resists both — because a peer warned in time, not because a rule stopped it. Status: ready. From: acttab1.
