# QA Walkthrough: apl-4 — Tester recruitment checklist

## Setup

- Open `ANDROID.md` at repo root. Navigate to Section 17.

## Smoke

- [ ] Section 17 header intact: `## Section 17 — Tester recruitment`.
- [ ] Stub placeholder text (`apl-4 content appends here`) is gone.
- [ ] Five subsections present: 17.1, 17.2, 17.3, 17.4, 17.5.

## 17.1 — Google Group creation

- [ ] Literal URL `https://groups.google.com/` appears as a
  hyperlink.
- [ ] Group name `Palateful Android Testers` literal.
- [ ] Group email `palateful-android-testers@googlegroups.com`
  literal.
- [ ] Access settings explicitly list:
  - Public / anyone can find
  - Anyone can join (ask to approve)
  - Members only can post
  - Members only can view conversations

## 17.2 — Play Console wiring

- [ ] Path literal: "Play Console → Testing → Internal testing →
  Testers".
- [ ] Opt-in URL format shown as code fence:
  `https://play.google.com/apps/internaltest/<app-specific-id>`.
- [ ] Explicit note that `<app-specific-id>` is generated by
  Play Console (operator reads the real value, does not guess).

## 17.3 — Outreach email template

- [ ] Template is inside a fenced code block.
- [ ] Template contains `<PASTE OPT-IN URL FROM SECTION 17.2 STEP 5>`
  placeholder.
- [ ] Template names the Google account matching caveat (same
  account on group + on phone).
- [ ] GitHub Issues feedback URL appears:
  `https://github.com/<your-user>/palateful/issues`.
- [ ] Reply-to email `leonid@ac93.org` appears in the template
  signature.
- [ ] Operator-facing notes below the template explain the
  `<your-user>` + opt-in URL placeholders.

## 17.4 — Expectation-setting block

- [ ] Subsection 17.4 body is inside a fenced code block
  (paste-ready).
- [ ] Text explicitly names: "at least 12 people" + "14 days".
- [ ] Text framed to testers, not operator.

## 17.5 — Operator-side target

- [ ] Subsection marked as operator-internal (not for testers).
- [ ] Invitation target: 15–20 invitations → 12 active testers.
- [ ] At least 3 tracking signals listed (Play Console Statistics,
  Crashlytics User segments, backend push_tokens count or similar).

## Cross-file sanity

- [ ] `grep -c 'palateful-android-testers@googlegroups.com' ANDROID.md`
  ≥ 2 (group creation step + Play Console wiring step).
- [ ] `grep -c 'leonid@ac93.org' ANDROID.md` still ≥ 10 (preserved
  existing + new outreach reference).

## Acceptance

All above checkboxes passing.
